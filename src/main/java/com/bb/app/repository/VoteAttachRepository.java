package com.bb.app.repository;

import com.bb.app.entity.VoteAttachEntity;
import org.springframework.data.jpa.repository.JpaRepository;

public interface VoteAttachRepository extends JpaRepository<VoteAttachEntity, Long> {
}
